import { Button } from '@/components/common/Button/Button';

export const TodoModalBtn = () => {
  return (
    <Button size="large" primary={true} className="mt-auto">
      확인
    </Button>
  );
};
